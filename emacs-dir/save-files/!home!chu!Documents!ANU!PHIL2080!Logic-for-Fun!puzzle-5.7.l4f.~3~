sort {
     blocks enum: b1, b2, b3.
     % blocks enum: b1, b2, b3, b4.
     % blocks enum: b1, b2, b3, b4, b5.

     block {}.
     state {}.
}

function {
     on: state, block -> block {}.
     move: state, block -> block {}.

     table: block {hidden}.
}

clause {
     % initial state ...
     % 3 blocks: b1 and b2 on the table, b3 on b2.

     % goal state ...
     % 3 blocks: b3 on the table, b1 on b3 and b2 on b1.

     % transitions ...

}

end
