

sort child enum: alice, boris, claire, david.


function age: child -> int {injective}.
function beans: child -> int {injective}.


% restrictions
clause age(x) > 5 AND age(x) < 10.
clause beans(x) > 0 AND beans(x) < 5.

% alice's age plus her beans is the same as boris's age plus his beans
clause age(alice) + beans(alice) = age(boris) + beans(boris).

% claire's age is two more than the number of alice's beans
clause age(claire) = beans(alice) + 2.

% alice's age is equal to the number of beans she and claire have altogether
clause age(alice) = beans(alice) + beans(claire).

% david is 8 years old and has 1 bean
